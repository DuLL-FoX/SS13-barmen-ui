# Server port (used in production and as proxy target in development)
PORT=3001

# Client development server port (only used during development with Vite)
CLIENT_PORT=3000

# GitHub token for accessing GitHub API (optional but recommended to avoid rate limits)
GITHUB_TOKEN=
